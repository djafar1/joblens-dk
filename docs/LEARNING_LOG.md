# Learning Log

## 2026-05-27

I started setting up JobLens DK today.

So far I have added the first version of the README and the project plan. The idea is to start simple with a classical NLP baseline, and then later build it up with embeddings, ML-based relevance scoring, SQL storage, RAG explanations, Docker, CI/CD, and deployment.

The first real goal is v0.1, where I just want to get the basic matching pipeline working without LLMs, database, or deployment.

Next step is to begin v0.1:

- Create the initial repository structure
- Add the first `src/` and `tests/` folders
- Start with basic text cleaning
- Add a small test for the text cleaner
